mixin cmd(s)
    p: a= '_' + s


#default.messages-group
    .header Help and Documentation
    .messages
        +msg('assets/img/avarel.jpg', 'Avalon', '_help')
        +msg('assets/img/logo.jpg', 'Gnar')
            +embed-desc
                .title Gnar Documentation
                .text This is all of GN4R-Bot's currently registered commands on the your guild.

                .fields
                .fields
                    .field
                        .field-title Moderation — 8
                        p Commands that help with the regulating normal server tasks.
                .fields
                    .field-inline
                        +cmd("ban")
                        +cmd("kick")
                        +cmd("unban")
                    .field-inline
                        +cmd("prune")
                        +cmd("deletemsg")
                        +cmd("enable")
                    .field-inline
                        +cmd("disable")
                        +cmd("listdisabled")

                .fields
                .fields
                    .field
                        .field-title Music — 12
                        p Listen to the bot's fabulous renditions of your favorite songs.
                .fields
                    .field-inline
                        +cmd("play")
                        +cmd("leave")
                        +cmd("pause")
                        +cmd("stop")
                    .field-inline
                        +cmd("skip")
                        +cmd("shuffle")
                        +cmd("nowplaying")
                        +cmd("queue")
                    .field-inline
                        +cmd("restart")
                        +cmd("repeat")
                        +cmd("reset")
                        +cmd("voteskip")

                .fields
                .fields
                    .field
                        .field-title Fun — 21
                        p Everything here is memes. Dank unfiltered memes. Batteries not included.
                .fields
                    .field-inline
                        +cmd("urbandict")
                        +cmd("coinflip")
                        +cmd("yodatalk")
                        +cmd("roll")
                        +cmd("poop")
                        +cmd("goodshit")
                        +cmd("8ball")
                    .field-inline
                        +cmd("leet")
                        +cmd("marvel")
                        +cmd("eyes")
                        +cmd("tts")
                        +cmd("champdata")
                        +cmd("answer")
                        +cmd("trivia")
                    .field-inline
                        +cmd("q")
                        +cmd("pbot")
                        +cmd("meme")
                        +cmd("cats")
                        +cmd("c&h")
                        +cmd("rcg")
                        +cmd("xkcd")

                .fields
                .fields
                    .field
                        .field-title General — 21
                        p General bot commands that every lame bot has. Have fun!
                .fields
                    .field-inline
                        +cmd("help")
                        +cmd("invite")
                        +cmd("ping")
                        +cmd("math")
                        +cmd("remindme")
                        +cmd("google")
                        +cmd("youtube")
                    .field-inline
                        +cmd("ptime")
                        +cmd("whois")
                        +cmd("info")
                        +cmd("donate")
                        +cmd("ascii")
                        +cmd("dialog")
                        +cmd("react")
                    .field-inline
                        +cmd("adventure")
                        +cmd("overwatch")
                        +cmd("league")
                        +cmd("game")
                        +cmd("poll")
                        +cmd("quote")
                        +cmd("ttb")

                .fields
                .fields
                    .field
                        .field-title Additional Information
                        p To view a command's description, do _help [command].
                    .field
                        .field-title News
                        p Check the news by using the bot on Discord.

                    .field-inline
                        .field-title Contact
                        p: a Website
                        p: a Discord Server
                    .field-inline
                        .field-title Donations
                        p: a PayPal
                        p: a Patreon
